// reducers
const initialState = {
  loading: false,
  list:[],
  details:{}
}

export const books = (state=initialState, action={}) => {
  switch(action.type){
    default:
      return state
  }
}    

export default books